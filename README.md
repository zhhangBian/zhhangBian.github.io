# Zhuohang Bian's Academic Homepage

Personal academic homepage built with Jekyll and deployed automatically through GitHub Actions.

## Local development

```bash
bundle install
bundle exec jekyll serve
```

The local site is available at <http://127.0.0.1:4000>. Pushes to `main` trigger the Pages build and deploy workflow for <https://zhhangbian.github.io/>.
